<!-- This view displays a specific contact that is called via their ID -->

<h2> Your specified contact: </h2>

<!-- The ID is entered via the URL search bar, that contact is called and displayed -->
<ul id="contact-center">
	<li><%= @contact.first_name %> <%= @contact.last_name %> <hr> <%= @contact.email %> <br> Note: <%= @contact.note %> <br> ID: <%= @contact.id %></li>
</ul>

<!-- Delete contact if this button is pressed -->
<form action="/contacts/<%= @contact.id %>" method="post">
	<input type="hidden" name="_method" value="delete">
	<input type="submit" name="Delete Contact" value="Delete Contact" class="hover">
</form>

<!-- Edit contact if this button is pressed -->
<form action="/contacts/<%= @contact.id %>/edit" method="get"> 
	<input type="submit" name="Edit Contact" value="Edit Contact" class="hover">
</form>
